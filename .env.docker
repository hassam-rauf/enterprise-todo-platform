# [Task]: T009 [From]: specs/phase4-k8s/containerization/plan.md §D4
# Docker environment variables — copy this file and fill in your values.
# Used by: docker compose -f k8s/docker-compose.yml up

# === Shared ===
DATABASE_URL=postgresql+asyncpg://user:password@host/dbname
BETTER_AUTH_SECRET=your-secret-key-here

# === Backend ===
ALLOWED_ORIGINS=http://localhost:3000
OPENAI_API_KEY=sk-your-openai-key
# MCP_SERVER_URL is set in docker-compose.yml (http://mcp-server:8001/sse)

# === Frontend (runtime — server-side only) ===
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000
