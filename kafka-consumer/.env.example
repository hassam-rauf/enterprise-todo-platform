# Kafka Consumer Service — Environment Variables
# Copy to .env and fill in values

# Kafka broker connection (comma-separated list for multiple brokers)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Optional topic name prefix (e.g. "prod-" makes topics "prod-task-events" etc.)
KAFKA_TOPIC_PREFIX=
